module.exports = function ($) {

  // Trace name (optional)
  // $.name = 'name of your trace';

  // URL to trace (optional, but must be set if using $.proxy)
  // $.root = 'http://localhost:3002/';

  // Local proxy port (optional)
  // $.proxy = 3003;
};